@page "/tools/text-decryptor"

<DtToolPageHeader ToolDefinition="@_toolDefinition"/>

<MudCard Class="my-4">
    <MudCardContent>
        <DtSection Id="config" Class="mt-0">
            <MudGrid Class="align-center" Spacing="4">
                <MudItem xs="12" md="4">
                    <DtTextField
                        T="string"
                        Value="@_args.Key"
                        Label="@_localizer["TextField.Key.Label"]"
                        OnDebounceIntervalElapsed="OnKeyChangedAsync"
                    />
                </MudItem>
                <MudItem xs="12" md="4">
                    <DtSelectEnum
                        T="TextDecryptorTool.EncryptionAlgorithm"
                        Label="@_localizer["Select.Algorithm.Label"]"
                        Value="@_args.Algorithm"
                        ValueToDisplay="@(type => Context.Localization.GetToolEnum(_toolDefinition, type))"
                        ValueChanged="OnAlgorithmChangedAsync"
                    />
                </MudItem>
                <MudItem xs="12" md="4">
                    <DtSelectEnum
                        T="TextDecryptorTool.InputFormat"
                        Label="@_localizer["Select.InputFormat.Label"]"
                        Value="@_args.InputFormat"
                        ValueToDisplay="@(type => Context.Localization.GetToolEnum(_toolDefinition, type))"
                        ValueChanged="OnInputFormatChangedAsync"
                    />
                </MudItem>
            </MudGrid>
        </DtSection>

        <DtSection Id="io" ShowDivider="true">
            <MudGrid Class="align-center" Spacing="4">
                <MudItem xs="12" md="6">
                    <DtTextField
                        T="string"
                        Value="@_args.EncryptedText"
                        Label="@_localizer["TextField.Input.Label"]"
                        Error="@(_result?.HasErrors ?? false)"
                        ErrorText="@(_result?.ErrorCodes.Select(it => Context.Localization.GetErrorCode(it)).FirstOrDefault(""))"
                        Lines="15"
                        OnDebounceIntervalElapsed="OnEncryptedTextChangedAsync"
                        LabelPosition="LabelPositionType.Inline"
                    />
                </MudItem>
                <MudItem xs="12" md="6">
                    <DtTextField
                        T="string"
                        Value="@GetDecryptedText()"
                        Label="@_localizer["TextField.Result.Label"]"
                        ReadOnly="true"
                        Lines="15"
                        LabelPosition="LabelPositionType.Inline"
                    />
                </MudItem>
            </MudGrid>
        </DtSection>
    </MudCardContent>
</MudCard>
